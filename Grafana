
mysql>SELECT
   mysql> UNIX_TIMESTAMP(time) as time_sec,
   mysql> temperature_2 as Termistor
mysql> FROM temperature 
mysql> WHERE arduinoID = 1
mysql> ORDER BY time ASC
// That is just an example, how to draw diagram in Grafana from your data located in MySQL database
